using System;

namespace NamingConvention
{
    class Program
    {   
        //Method name start with capital but name of arguments and variables start with lower case and first alphabet of the second word in the same variable name or the argument should be upper case
        //do not start with numbers 
        //Contain only symbol that is underscore(_)
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
