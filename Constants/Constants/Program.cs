using System;

namespace Constants
{
    class Program
    {
        const int days = 365;
        // Use keyword const before declaring all the datatypes to keep them as constant throughout the program....
        static void Main(string[] args)
        {
            Console.WriteLine("There are {0} days in a year",days);
            Console.Read();
        }
    }
}
